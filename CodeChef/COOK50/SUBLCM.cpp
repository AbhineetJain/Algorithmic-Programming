#include<bits/stdc++.h>

int main()
{
	long long int tc, n;
	long long int arr[100005];
	scanf("%lld", &tc);
	while(tc--)
	{
		scanf("%lld", &n);
		for(int i=0; i<n; i++)
			scanf("%lld", &arr[i]);
	}
	return 0;
}
