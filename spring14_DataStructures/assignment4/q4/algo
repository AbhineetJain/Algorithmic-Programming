Check using dfs
Do another field 'frequency'
Do Dijkstra's using minHeap(insert Source, then delete it and insert its neighbours, increase their frequency by 1, and update their weights, if you encounter the dest twice as min, break and print its weight)
